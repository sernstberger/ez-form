# SDD ledger — plan: docs/superpowers/plans/2026-09-01-ez-form-v2.1.md
Spec: docs/superpowers/specs/2026-09-01-ez-form-v2.1-grouping-money-design.md (binding).
Workspace ruling: in place on feat/v1 (same as v2). Models: T1 sonnet, T3 sonnet (worktree), T2 opus; reviews sonnet/opus; final review fable.
## Pre-flight scan
| Pair / task | Produces vs consumes | Found |
|---|---|---|
| T1 -> T2 | getSeparators/groupWhileTyping signatures per spec; T2 imports them | match |
| T3 -> T2 | MoneyField.test.tsx exists; T2 appends the live-grouping case | match (T2 ordered after T3) |
| T3 internal | MoneyField omits format/locale; spec pins en-US/USD; contract componentName MoneyField via useEzFormContext | consistent |
| Rubric | no empty tests / duplicated logic mandated | clean |
## Execution
T1 + T3 dispatched in parallel, BASE 5e64ffd.
T1: implementer DONE (a55bab6, 142/142); reviewer (sonnet) dispatched.
T3: implementer DONE (worktree cc7dab3, cherry-picked 1d00abe; 150/150). Reviewer (sonnet) dispatched. T2 dispatched (opus) on main, BASE 1d00abe.
T3: complete (commits a55bab6..1d00abe, review clean)
T1: minor (deferred): T2 reviewer should confirm repeated per-keystroke groupWhileTyping calls give sane caret behavior at the integration point.
T1: complete (commits 5e64ffd..a55bab6, review clean)
T2: implementer DONE_WITH_CONCERNS (29dd5ba, 155/155): paste not grouped live (Base UI onPaste preventDefault); spec's backspace-over-separator sentence unachievable; reviewer (opus) dispatched.
T2: browser check (controller, MoneyField default story): live grouping 1,234,567.5; mid-insert caret correct (198|,234,567.5); backspace OK; blur → $198,234,567.00; no console errors.
T2: review Needs fixes — CRITICAL: caret jumps to end when regrouped text equals current inputValue (separator delete); effect keyed on [inputValue]. Minors: spec backspace sentence wrong; comment says target.value (Base UI reads currentTarget.value).
T2: Ruling: paste not grouped live — FOLLOW-UP (Base UI onPaste preventDefaults and restores its own caret; correct fix is an onPasteCapture design change). Cost if wrong: pasted numbers show commas only on blur.
T2: fix round 1/5 opened (1 critical + 2 minors bundled); implementer resumed.
T2: fix round 1 commit d5568b7 (157/157); scoped re-review (sonnet) dispatched.
T2: browser check of fix (NumberField default): Backspace at 1,|000 keeps caret after 1 (typing 7 → 17,000 caret held); Delete at 17|,000 same (→ 175,000). OK.
Final review (fable) dispatched on review-5e64ffd..d5568b7.diff with a DRY lens over all of src (Steve's request), in parallel with T2 re-review.
T2: fix round 1/5 (3 addressed, 0 open; commit d5568b7)
T2: minor (deferred): paste not grouped until blur (follow-up, design change); useMemo on inline format literal recomputes (cheap).
T2: complete (commits 1d00abe..d5568b7, review clean after 1 fix round)
Final review (fable): With fixes. Important: MoneyField shows $20.00 but submits 19.999 (no maximumFractionDigits) → fix: USD_FORMAT const with maximumFractionDigits: 2 + test + README. Minors to fix now: duplicate outside-Form test in MoneyField.test; stale comment in NumberFieldControl; README NumberField live-grouping/opt-out clause + paste-groups-on-blur note; isComposing guard (one line).
DRY audit (all src): 8 items; only #1 (Form story decorator copy-pasted in 8 story files) recommended now → global decorator in .storybook/preview.tsx keyed on parameters.form. Ruling: include in fix wave (no reason for the duplication; Storybook-native; stories only). #2-#8 leave (shape-only duplication carrying per-field facts; hand-rolled pieces have no public equivalent). Cost if wrong: revert one preview.tsx + 8 story files.
Ruling: groupWhileTyping locale hardening (leading minus, space-group locales) and paste grouping → FOLLOW-UP (not reachable from MoneyField/en-US).
Final fix wave dispatched (opus), FIX_BASE d5568b7.
Final fix wave: commit 7b8c0d3 (157/157, build + build-storybook OK, 45 story ids identical). Scoped re-review (opus) dispatched.
Fix wave visual check: textfield--with-error (play runs), autocomplete--async-options, moneyfield--min-max all render one Form via the global decorator; no console errors.
Final fix wave: re-review clean (5/5 addressed, no breakage). v2.1 complete.
