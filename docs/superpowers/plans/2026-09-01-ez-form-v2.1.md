# ez-form v2.1 implementation plan — live grouping + MoneyField

Spec: `docs/superpowers/specs/2026-09-01-ez-form-v2.1-grouping-money-design.md` (binding). Global constraints: identical to v2 (`docs/superpowers/plans/2026-09-01-ez-form-v2.md` → Global Constraints); the v2 SDD workspace copy is gone, so read them from the v2 plan. Additional constraint: **no new dependencies**.

Dependency order: Task 1 ∥ Task 3, then Task 2 (Task 2 also adds MoneyField's live-grouping test, so it runs after Task 3).

### Task 1: `groupWhileTyping` helper

**Files:**
- Create: `src/fields/NumberField/groupWhileTyping.ts`, `src/fields/NumberField/groupWhileTyping.test.ts`

**Interfaces:** exactly the spec's `Separators`, `getSeparators`, `GroupedText`, `groupWhileTyping`.

- [ ] **Step 1 (RED):** write `groupWhileTyping.test.ts` as a table-driven `it.each` over the spec's acceptance table (all ten rows) using `getSeparators('en-US')`, plus the two `getSeparators` cases. Run `pnpm vitest run src/fields/NumberField/groupWhileTyping.test.ts` — fails (module missing).
- [ ] **Step 2 (GREEN):** implement per the spec's Rules. `getSeparators` uses `new Intl.NumberFormat(locale, format).formatToParts(1234567.891)` and reads the `group` and `decimal` parts (fall back to `','`/`'.'` if a part is absent, e.g. integer-only formats omit `decimal`). Caret mapping counts significant characters as the spec defines.
- [ ] **Step 3:** `pnpm typecheck && pnpm test`; commit `feat(NumberField): groupWhileTyping helper`.

### Task 2: live grouping in `NumberFieldControl`

**Files:**
- Modify: `src/fields/NumberField/NumberFieldControl.tsx`, `src/fields/NumberField/NumberField.test.tsx`, `src/fields/MoneyField/MoneyField.test.tsx`
- Consumes: Task 1 helper; Task 3 `MoneyField`.

- [ ] **Step 1 (RED):** add the four `NumberField.test.tsx` cases from the spec's Acceptance and the MoneyField "typing `1234` shows `1,234` before blur" case. Use `await user.type(input, '1234567')` then `expect(input).toHaveValue('1,234,567')` **without** blurring; then submit and assert the number. Run the two files — the live assertions fail (Base UI shows raw text until blur).
- [ ] **Step 2 (GREEN):** implement the spec's "NumberFieldControl wiring" section: `separators` memoized in `NumberFieldControl` (`useMemo` on `locale`, `format`; `null` when `format?.useGrouping === false`), passed to `NumberInput`; `onChange` in the `inputProps` chain rewrites `e.target.value` + stores the caret in a ref before calling `rest.onChange`; `useLayoutEffect([inputValue])` restores the caret with `setSelectionRange` when `document.activeElement` is the input. Keep `NumberInput` module-level and hook order stable.
- [ ] **Step 3:** `pnpm typecheck && pnpm test`, then check `fields-numberfield--default` and `fields-moneyfield--default` in the running Storybook (:6006): type `1234567.5`, confirm `1,234,567.5` appears while typing and the caret stays at the end; place the caret after the first digit and type `9`, confirm the caret stays after the `9`. Commit `feat(NumberField): group digits while typing`.

### Task 3: `MoneyField`

**Files:**
- Create: `src/fields/MoneyField/MoneyField.tsx`, `index.ts`, `MoneyField.test.tsx`, `MoneyField.stories.tsx`
- Modify: `src/index.ts`, `README.md`

- [ ] **Step 1 (RED):** `MoneyField.test.tsx` per the spec's Acceptance **except** the live-grouping case (Task 2 adds it): `describeFieldContract` block, blur-format + submit case, outside-`<Form>` case, `min={0}` case. Run it — fails (module missing).
- [ ] **Step 2 (GREEN):** implement per the spec's `MoneyField` section (`useEzFormContext('MoneyField')` first, then `NumberField` with the pinned `locale`/`format` and default steps, consumer props spread last). `index.ts` = `export * from './MoneyField'`. Add `export { MoneyField, type MoneyFieldProps } from './fields/MoneyField'` to `src/index.ts`.
- [ ] **Step 3:** stories `Fields/MoneyField` (`Default`, `Required`, `MinMax`) following `NumberField.stories.tsx`'s single-meta-decorator + `parameters.form` pattern; spies hoisted.
- [ ] **Step 4:** README: Components table row; rules table — add `MoneyField` to the `required`, `min`/`max`, `validate` rows; a "MoneyField" subsection (USD only, value in dollars as a number, grouped while typing, `$1,234.50` on blur) with a `z.number().min(0)` example.
- [ ] **Step 5:** `pnpm typecheck && pnpm test && pnpm build && pnpm build-storybook`; confirm `dist/index.d.ts` exports `MoneyField`/`MoneyFieldProps`. Commit `feat: MoneyField (USD) on NumberField`.
