# v5 wave — form feedback & a11y, plus small housekeeping

Spec: none; authority is each GitHub issue plus the rulings here and `docs/PHILOSOPHY.md`
(rules 1–5 and the "a component ships when" checklist are binding). Base: 9b45774.

## Global Constraints

- `docs/PHILOSOPHY.md` rule 2: no `sx`, ripple props, or theme-unreachable literals in
  `src/`. Defaults via `useDefaultProps` + `styled` slots + `<name>Classes` +
  `src/theme/augmentation.ts`. A slot default spread under `slotProps` is the pattern.
- Rule 1: extend MUI / Base UI / react-hook-form types; never re-implement. Audit before changing.
- Rule 3: every component test keeps its jest-axe pass; pristine output (no `act()` warnings,
  no console noise — dev warnings added by Task 5 must be silenced in tests that trigger them
  on purpose and must not fire anywhere else in the suite).
- Rule 4: the form owns the lifecycle; fields read from context.
- **i18n-ready strings (for #23):** every new user-visible default string is a prop with a
  default value (`fooText = '…'`), never an inline literal in JSX, so #23 can route it
  through a locale table later. Announcement strings that interpolate values take a
  function prop (`(ctx) => string`) with a default implementation.
- A consumer's explicit prop always wins over any default this wave adds.
- Tests: vitest + Testing Library + jest-axe. `pnpm typecheck && pnpm test` before every
  commit; `pnpm format`; `node scripts/check-guardrails.mjs` clean.
- README: a row/section for any new prop or behaviour a consumer can see.
- Commit per task, conventional prefix, `Closes #N` (one `Closes` line per issue).
- No subagents from implementers. Do not touch `.superpowers/`, other worktrees, or `main`.

## Tracks

| Track | Worktree | Tasks | Issues |
|---|---|---|---|
| A live | `.worktrees/v5-live` | 1, 2 | #3, #2 |
| B order | `.worktrees/v5-order` | 3 | #5 |
| C input | `.worktrees/v5-input` | 4 | #6, #7 |
| D warn | `.worktrees/v5-warn` | 5 | #8 |
| E required | `.worktrees/v5-required` | 6 | #4 |
| F fixes | `.worktrees/v5-fixes` | 7 | #71, #83 |
| G dialog | `.worktrees/v5-dialog` | 8 | #22 |
| H phone | `.worktrees/v8-phone` | 9 | #16 |
| I zipstate | `.worktrees/v8-zipstate` | 10 | #18, #19 |
| J ssn | `.worktrees/v8-ssn` | 11 | #17 |
| K address | `.worktrees/v8-address` | 12 | #20 |
| L examples | `.worktrees/v8-examples` | 13 | Insurance/Checkout adoption |
| M switch | `.worktrees/v5-switch` | 14 | Checkbox vs Switch docs (Steve) |
| N upload | `.worktrees/v8-upload` | 15 | #15 |
| O strict | `.worktrees/v5-strict` | 16 | strict mode / zero warnings (Steve; #84) |

---

### Task 1: Shared live region + async submit status announcements (#3)

Files: `src/Form/LiveRegion.tsx` (new), `src/Form/Form.tsx`, `src/Form/Form.test.tsx`,
`src/Form/Form.stories.tsx`, `src/theme/augmentation.ts`, `src/index.ts`, README.

Requirements:
1. Audit first: `FieldArray`, `PasswordStrength`, `ResendCodeButton` and the SignUp example
   already render ad-hoc `aria-live` / `role="status"` nodes. Build one `LiveRegion`
   component (`EzLiveRegion`: `styled` root with a visually-hidden default style block so a
   theme can override it; `liveRegionClasses`; augmentation entry) plus a tiny
   `useAnnounce()`-style hook or `<LiveRegion message>` API — pick the simpler one that the
   three existing call sites can adopt. Migrate those three `src/` call sites to it (the
   example may stay). `politeness` defaults to `polite`.
2. `Form` renders one `LiveRegion` (inside the form, after the children, before the actions
   slot if there is one) and announces: submit started, submit succeeded, submit failed.
   Strings are props on `Form` with defaults: `submitPendingText = 'Submitting…'`,
   `submitSuccessText = 'Submitted.'`, `submitErrorText = 'Submit failed.'`; `false`
   suppresses an individual message. Announce via the existing lifecycle (the `isSubmitting`
   / `isSubmitSuccessful` / `onSubmitError` plumbing already in `Form.tsx`); do not add a
   second submit tracker. A validation failure (schema rejects, no submit call) is NOT
   "submit failed" — `FormErrorSummary` already covers it; announce nothing there.
3. The same message twice in a row must re-announce (clear then set, or key the node) —
   a second failed submit must be heard.
4. Tests: each of the three messages appears in the live region under the matching
   lifecycle; `false` suppresses; repeat announcement re-fires; axe clean; the migrated
   components' existing tests pass unmodified.
5. Export `LiveRegion`, its props type and `liveRegionClasses` from `src/index.ts`.

### Task 2: Focus and announcement on wizard step change (#2)

Depends on Task 1 (`LiveRegion`). Files: `src/Wizard/Wizard.tsx`, `src/Wizard/WizardStep.tsx`,
`src/Wizard/Wizard.test.tsx`, README Wizard section.

Requirements:
1. When the current step changes through Next/Back/`goTo` (not on initial mount, not on a
   failed-submit jump that `FormErrorSummary` already focuses), focus moves to the step's
   heading (the legend heading `WizardStep` renders, or the `aria-labelledby` target in the
   vertical layout). Where there is no heading element, focus the step's fieldset with
   `tabIndex={-1}`. Focus the container, not the first field: APG guidance for multi-step
   forms, and focusing a field would announce it before its step context.
2. Announce through the shared `LiveRegion`: default `stepAnnouncement =
   ({ index, count, label }) => \`Step ${index + 1} of ${count}${label ? `, ${label}` : ''}\``;
   `index`/`count` are computed over the *effective* (conditional `when`-filtered) step list.
   `stepAnnouncement={false}` disables it. Prop lives on `Wizard`.
3. The failed-final-submit jump (navigates to first errored step) must still let
   `FormErrorSummary` take focus — assert focus is on the summary heading in that case, not
   the step heading.
4. Tests: focus lands on the heading after Next and Back; announcement text for both
   layouts; conditional-steps count excludes hidden steps; `false` disables; initial mount
   does not steal focus; axe clean.

### Task 3: `actionsOrder` for WizardNav and ConfirmDialog (#5)

Files: `src/Wizard/WizardNav.tsx`, `src/ConfirmDialog/ConfirmDialog.tsx`, their tests and
stories, `src/theme/augmentation.ts`, README.

Requirements:
1. `ConfirmDialog` gains `actionsOrder?: 'cancel-confirm' | 'confirm-cancel'` (default
   `'cancel-confirm'`, Material). `WizardNav` gains `actionsOrder?: 'back-next' | 'next-back'`
   (default `'back-next'`). Both flow through the component's existing `useDefaultProps`
   registration (`EzConfirmDialog`, `EzWizardNav`) so `theme.components.<key>.defaultProps
   .actionsOrder` sets it globally; add the prop to the augmentation types if the props
   interface is not already what `ComponentsPropsList` points at.
2. The order is realised by rendering the buttons in a different DOM order — no
   `flex-direction: row-reverse`, no `order:` — so tab order matches visual order.
   `ConfirmDialog`'s "Cancel gets `autoFocus`" rule is unaffected by order.
3. `Form confirm`, `ClearButton confirm` and `useConfirm` all go through `ConfirmDialog`;
   confirm that a theme-level `EzConfirmDialog.defaultProps.actionsOrder` reaches all of them
   (one test through `Form confirm` is enough).
4. Tests: DOM order of the two buttons for each value; theme `defaultProps` path; axe clean.
   Story: one per component showing the reversed order.

### Task 4: `autoComplete` and `inputMode` defaults by field type (#6, #7)

Files: `src/fields/TextField/TextField.tsx`, `NumberField`, `MoneyField`, `OtpField`,
`PasswordField` (audit only), the pickers (audit only), their tests, README.

Requirements:
1. `TextField`: derive defaults from `type` when the consumer sets neither prop:
   `email` → `autoComplete="email"`, `inputMode="email"`; `tel` → `"tel"`/`"tel"`;
   `url` → `"url"`/`"url"`; `search` → `inputMode="search"`. Other types: no default
   (do not guess `name` → token; a wrong token is worse than none).
2. `NumberField` → `inputMode="decimal"` unless the field is integer-only (no fractional
   `step` / a `format` with 0 fraction digits — use whatever the component already knows),
   then `numeric`. `MoneyField` → `inputMode="decimal"`. `OtpField` → `inputMode="numeric"`
   and `autoComplete="one-time-code"` (check what Base UI already sets; do not duplicate).
3. `PasswordField` already sets `autoComplete="current-password"` — leave it; README shows
   `autoComplete="new-password"` for sign-up (verify the SignUp example does this; fix if
   not).
4. Attributes go on the real `<input>` through the existing `slotProps.htmlInput` /
   `inputProps` path each component already uses; a consumer's explicit `autoComplete` /
   `inputMode` (top-level or inside `slotProps.htmlInput`) always wins.
5. Tests: attribute on the DOM input for each mapping; consumer override wins; nothing
   emitted for plain `type="text"`.
6. README: a short "Mobile keyboards & autofill" table listing the defaults. Note the v8
   fields (#16–#19) will extend the table.

### Task 5: Dev-mode a11y warnings (#8)

Files: `src/devWarn.ts` (new), `src/fields/useEzField.tsx`, `src/fields/Option.ts` (or the
option-consuming fields), `src/Wizard/Wizard.tsx`, tests, README (Developer warnings section).

Requirements:
1. `devWarn(key, message)`: `console.warn` once per `key` per session, only when
   `process.env.NODE_ENV !== 'production'`; the check must be statically strippable
   (a top-level `const isDev = process.env.NODE_ENV !== 'production'` guarded call) so the
   production bundle carries no strings. Export nothing public.
2. Warn when: (a) a field mounts through `useEzField` with no accessible name — no `label`,
   no `aria-label`, no `aria-labelledby` (accept the props the hook already sees; do not
   query the DOM); (b) `Select`, `RadioGroup`, `Autocomplete`, `ToggleButtonGroup`,
   `CheckboxGroup` receive `options` with duplicate `value`s (one shared helper called from
   each); (c) a wizard step's `fields` lists a name that is not mounted when Next validates
   it — use `control._names.mount` (RHF's registered-names set; note in a comment that it is
   an internal-but-stable API) and skip the check when the set is empty.
3. Tests: each warning fires once with the field/step name in the message; does not fire
   for the valid case; a `NODE_ENV=production` test (stub `process.env`) fires nothing.
   Suite-wide: the rest of the test run must not emit any new warnings — run the whole
   suite and fix any story/test fixture that trips (a) or (b) rather than muting it.

### Task 6: Required indication beyond the asterisk (#4)

Files: `src/Form/Form.tsx`, `src/Form/Form.test.tsx`, `src/Form/Form.stories.tsx`, README.

Requirements:
1. `Form` already has `requiredIndicatorText` (rendered only in `requiredIndicator=
   "optional"` mode, default "All fields are required unless marked optional."). Extend it to
   `asterisk` mode with a mode-dependent default: `'Fields marked * are required.'`. The
   existing rendering path (description slot / legend) is reused; `false` hides it in either
   mode; an explicit string is used verbatim in either mode.
2. The `*` inside the instruction is visual only: wrap it so AT reads the sentence sensibly
   (MUI's `FormLabel` asterisk is `aria-hidden`; mirror that — e.g. `<span aria-hidden>*</span>`
   with a visually-hidden "asterisk" alternative, or phrase it as "Required fields are marked
   with an asterisk (*)"). Pick one; the ruling below defaults to the phrasing approach.
3. Per-field: no extra hidden "required" text — the input already carries `required` /
   `aria-required`, so AT announces it; adding text would double-announce.
4. Tests: default text in asterisk mode; `false` hides; custom string; optional-mode default
   unchanged; existing Form/Wizard/example tests that snapshot or query form text are
   updated, not weakened; axe clean. Story updated.

### Task 7: React 18 `ref` support and picker `clearable` after unparsable paste (#71, #83)

Files: `src/Form/Form.tsx`, `src/FormSection/FormSection.tsx`, `src/fields/pickers/
usePickerField.ts`, picker tests, README (Install note; Date pickers section).

Requirements (#71):
1. Wrap `Form` and `FormSection` with `forwardRef` so the `ref` reaches the imperative handle /
   fieldset on React 18 and 19 alike; keep the exported props types unchanged for consumers
   (`ref` stays typed as today). Generic `Form<TIn, TOut>` must keep its generics through
   `forwardRef` (cast the result the way MUI does for generic components, with a comment).
   Peer range stays `^18 || ^19`.
2. README Install: one sentence that `ref` works on both React majors.
3. Test: a `ref` callback receives the handle/fieldset (already covered? if so, assert the
   component is a `forwardRef` exotic via `$$typeof`, or skip with a note in the report).

Requirements (#83):
4. When a consumer passes `clearable` (via `slotProps.field`/`textField` or the flat prop MUI X
   exposes), clearing after an unparsable paste resets `pickerError` and re-runs the field's
   error derivation so the field shows no error. Hook into the `onClear` MUI X exposes
   (`slotProps.clearButton` / `onClear`), merging with a consumer's own handler.
5. README Date pickers: document that `required` takes precedence over `invalidDate` when an
   unparsable value is pasted into a required, empty picker (`ezResolver` order) and that
   submit remains blocked either way.
6. Tests: `clearable` + unparsable paste + clear → no error text, `aria-invalid` false;
   consumer `onClear` still called.

### Task 8: `FormDialog` — form in a MUI Dialog with exit confirmation (#22)

Files: `src/FormDialog/FormDialog.tsx` (new), `FormDialog.test.tsx`, `FormDialog.stories.tsx`,
`index.ts`, `src/theme/augmentation.ts`, `src/index.ts`, README (new section + Components row).

Requirements:
1. `FormDialog` = MUI `Dialog` whose paper is the `<Form>` element: props are
   `Omit<DialogProps, 'onClose' | 'open' | 'slotProps'> & FormProps` plus `open`,
   `onClose`, `title` (rendered as `DialogTitle`, wired to `aria-labelledby`), `actions`
   (rendered in `DialogActions`; defaults to a `ClearButton`-free `SubmitButton` + a Cancel
   `Button`), and `exitConfirm?: ConfirmOptions | false`. Use `slotProps.paper = { component:
   Form, ... }` if it composes cleanly; otherwise render `Form` as the first child of
   `Dialog` with `DialogContent` inside. Audit `Form`'s `component`/`slotProps` first.
2. Closing while dirty (backdrop click, Escape, Cancel) opens `ConfirmDialog` with
   `exitConfirm` copy (default title "Discard changes?", confirm "Discard", cancel
   "Keep editing"); confirming calls `onClose`; not dirty → close immediately. Reuse
   `useConfirm` and the form's own dirty state (`useFormGuard` semantics: not dirty after a
   successful submit). `exitConfirm={false}` disables the prompt.
3. Successful submit closes the dialog (`onClose` with reason `'submit'`) unless
   `closeOnSubmit={false}`. Loading/disabled/confirm on `Form` keep working unchanged.
4. Theme: `EzFormDialog` with `useDefaultProps`, `styled` slots (`Root`, `Title`, `Content`,
   `Actions`), `formDialogClasses`, augmentation entry.
5. Tests: dirty → Escape prompts; confirm discards; cancel keeps; clean → Escape closes;
   submit closes; `exitConfirm={false}`; focus returns to the opener after close; axe clean
   (dialog open). Stories: basic, with confirm, long content scroll.

### Task 9: `PhoneField` — US phone with formatting (#16)

Files: `src/fields/PhoneField/PhoneField.tsx` (+ `index.ts`, test, stories), `src/theme/
augmentation.ts` (only if a slot/default registers), `src/index.ts`, README (Components row +
"US fields" section, and a row in the Mobile keyboards & autofill table).

Requirements:
1. Props: `Omit<TextFieldProps, 'type' | 'inputMode' | 'autoComplete' | 'pattern'> &
   { format?: string; invalidMessage?: ReactNode; autoComplete?: string }` where
   `format` is a `#` template, default `'###-###-####'`; `autoComplete` defaults to `'tel'`
   (consumer may pass `'shipping tel'` etc.); `inputMode="tel"`; `type="tel"`.
2. **Form value is digits only** (`'5555555555'`, a string); the displayed text is the
   formatted template applied to the digits typed so far (`'555-55'` while typing). Typing,
   pasting (`(555) 555-5555`, `+1 555 555 5555` → leading `1` on an 11-digit paste is
   dropped), deleting and Backspace over a separator all work; caret stays with the digit
   the user is editing (compute the digit index before formatting and restore the caret
   after — the NumberField `groupWhileTyping` code is the reference for the technique).
   Never accept more digits than the template has `#`s.
3. Built-in rule (composed like `required`/`pattern` through the field's `rules`): when the
   value is non-empty and shorter than the template's digit count, the error is
   `invalidMessage` (default `'Enter a 10-digit phone number'` — derive the count from the
   template so a custom template gets a matching default message).
4. Bound through `useEzField`/`FieldFrame` exactly like `TextField` (extend it; do not
   re-implement the frame). Empty → `''`, never `undefined`, so `required` still applies.
5. Tests: typing formats progressively; paste variants normalise; value stored as digits;
   caret preserved when editing the middle; incomplete → `invalidMessage`; empty +
   `required` → required message; consumer `autoComplete` override; `describeFieldContract`;
   axe. Stories: default, custom template `(###) ###-####`, with error.

### Task 10: `ZipField` and `StateSelect` (#18, #19)

Files: `src/fields/ZipField/*`, `src/fields/StateSelect/*` (each with `index.ts`, test,
stories), `src/index.ts`, README (rows + US fields section + keyboards table).

Requirements:
1. `ZipField`: `Omit<TextFieldProps, 'type' | 'inputMode' | 'autoComplete' | 'pattern'> &
   { invalidMessage?: ReactNode; autoComplete?: string }`. Accepts digits only, max 5 (strips
   anything else on type/paste); `inputMode="numeric"`, `autoComplete` default
   `'postal-code'`; value is the digit string; built-in rule: non-empty and not 5 digits →
   `invalidMessage` (default `'Enter a 5-digit ZIP code'`). Same binding and empty-value
   rules as Task 9 point 4.
2. `StateSelect`: `Omit<SelectProps, 'options'> & { territories?: boolean; autoComplete?:
   string }`. Options are the 50 states + DC (values = USPS abbreviations, labels = full
   names), plus PR, GU, VI, AS, MP when `territories` is true; exported `US_STATES` and
   `US_TERRITORIES` `Option` arrays. `autoComplete` default `'address-level1'` reaches the
   native input MUI Select renders for autofill (`slotProps.input`/`inputProps` — verify
   which path MUI 9 Select uses and note it).
3. Both bound like their base fields; no re-implementation of `TextField`/`Select`.
4. Tests: Zip strips letters, caps at 5, invalid message, required; StateSelect renders 51
   options by default / 56 with territories, value is the abbreviation, autoComplete on the
   input; `describeFieldContract` for both; axe. Stories: one each, plus territories.

### Task 11: `SsnField` (#17)

Files: `src/fields/SsnField/*`, `src/index.ts`, `src/theme/augmentation.ts` (if a reveal
slot registers), README.

Requirements:
1. Props: as Task 9 but template fixed `'###-##-####'`, `autoComplete="off"`,
   `inputMode="numeric"`, value = 9 digits, `invalidMessage` default
   `'Enter a 9-digit Social Security number'`, and `reveal?: boolean` (default `true`) that
   adds the same show/hide adornment `PasswordField` uses (extract/share its toggle rather
   than copying it; audit `PasswordField` first). Hidden state renders `type="password"`;
   revealed renders the formatted text.
2. Same formatting/caret/paste behaviour as `PhoneField` — share the template formatter
   (`src/fields/formatTemplate.ts` or similar) with Task 9 if both are on the branch;
   otherwise implement it identically and note the merge duplicate in the report so the
   controller dedupes.
3. Tests: hidden by default, reveal toggles, digits-only value, invalid/required messages,
   axe; story.

### Task 12: `AddressField` — US composite (#20)

Depends on Task 10 (`StateSelect`, `ZipField`) being on `main`. Files: `src/fields/
AddressField/*`, `src/theme/augmentation.ts`, `src/index.ts`, README.

Requirements:
1. `<AddressField name="address" />` renders five bound parts under the nested object
   `name`: `${name}.street` (TextField, `street-address`), `${name}.street2` (TextField,
   `address-line2`, optional, hidden with `street2={false}`), `${name}.city` (TextField,
   `address-level2`), `${name}.state` (StateSelect), `${name}.zip` (ZipField). Each part is
   the real field component, so per-part errors, `required`, `disabled` and focus-on-error
   work unchanged. `required` on `AddressField` applies to street/city/state/zip (never
   street2). `autoCompleteSection?: 'shipping' | 'billing' | string` prefixes every token.
   Labels are props with defaults (`streetLabel = 'Street address'`, …); `legend` optional
   (renders a `FormSection`-style fieldset when given).
2. Registered as `EzAddressField`: `useDefaultProps`, `styled` slots `Root` (layout
   container; default style block = a responsive grid so a theme can override), `Row`/
   part slots as needed, `addressFieldClasses`, augmentation entry. No `sx`.
3. Exports the value type `AddressValue` and a zod helper `addressSchema({ street2?:
   boolean })` returning the matching `z.object` (strings; `state` and `zip` non-empty) so
   examples do not restate it.
4. Tests: all parts render with the right names/autocomplete; `required` propagates; per-part
   error appears on the right part; `street2={false}`; theme override reaches the root;
   `describeFieldContract` where the contract applies to a composite (skip parts that assume
   a single input, with a comment); axe. Stories: default, shipping section, with errors.

### Task 13: Examples adopt the US fields (Insurance, Checkout)

Depends on Tasks 9, 10, 12 on `main`. Files: `src/examples/Insurance/*`, `src/examples/
Checkout/*`, README examples section if it lists the fields used.

Requirements:
1. Insurance: `phone` becomes `<PhoneField name="phone" required />` and the schema's
   `.regex(/^\d{3}-\d{3}-\d{4}$/…)` is deleted (schema keeps the non-empty check; the
   field's built-in rule owns completeness). The address block becomes
   `<AddressField name="address" required legend=…>` (or `street2={false}` if the layout
   wants four parts) with the schema from `addressSchema`; the saved-state / seeded
   localStorage fixtures and the Review step's `ReadOnlyField`s gain `address.state` (show
   the state abbreviation; if `ReadOnlyField` cannot resolve a Select label, show the value).
   Stories and tests updated; the Review-step seeded tests keep their timing approach.
2. Checkout: the US branch of the state field uses `StateSelect` (delete
   `US_STATE_OPTIONS`); postal code stays a `TextField` because the example is
   multi-country — add a one-line comment saying why `ZipField` is not used there.
3. Audit the other examples (Login, SignUp, Profile, Loan) for phone/zip/SSN-shaped
   `TextField`s with a `pattern` and convert them the same way; list what you converted.
4. All example tests, stories and the guardrail script stay green.

### Task 14: Checkbox vs Switch — usage rule in docs, examples corrected (Steve, Sept 2)

Files: README (new "Checkbox vs Switch" section next to the Components table, mirroring the
Date pickers "When to use which" style), `src/fields/Checkbox/Checkbox.tsx` and
`src/fields/Switch/Switch.tsx` JSDoc, `src/examples/Insurance/Insurance.tsx` (+ test/story),
`src/examples/Profile/Profile.tsx` (+ test/story), Switch/Checkbox stories.

Requirements:
1. README section states the rule and the reasoning in one short table:
   - **Checkbox**: a yes/no answer or opt-in that is *recorded when the form is submitted*
     ("I accept the terms", "Same as shipping", "Insure a vehicle"); anything a user may
     review before committing; one of several independent options (`CheckboxGroup`).
   - **Switch**: a setting that *takes effect immediately* with no submit step (dark mode,
     notifications on a settings page that autosaves, a UI mode toggle whose `onChange`
     does the work). If the page has a Submit button, it is almost always a Checkbox.
   - Both: label phrased as the state when on ("Marketing emails"), never as a question.
   Cite Material Design's selection-controls guidance and WCAG: a switch exposes
   `role="switch"`, and AT announces "on/off", which is wrong for an unsubmitted answer.
2. Same rule as a one-paragraph JSDoc on both components (`@remarks When to use`), and each
   component's Storybook docs description (the `parameters.docs.description.component`
   or the story file's JSDoc — follow what other fields do).
3. Insurance: `hasVehicle` becomes `<Checkbox name="hasVehicle" label="I want to insure a
   vehicle" />` (conditional-fields behaviour unchanged); Profile: `marketingEmails` becomes
   a `Checkbox`. Update their tests/stories/seeded state where the role/label is queried.
   The Switch stories keep a realistic immediate-effect example (e.g. a toggle whose
   `onChange` flips something visibly, no submit).
4. Guardrails, typecheck, tests, stories green.

### Task 15: `FileField` drop zone, limits, progress hooks (#15)

Files: `src/fields/FileField/FileField.tsx` (+ test, stories), `src/theme/augmentation.ts`,
README FileField row/section.

Requirements:
1. Audit first: `FileField` already registers `EzFileField` (`useDefaultProps`, `styled`
   slots, `fileFieldClasses`, a chip list, a picker `Button` over a hidden input). Extend it;
   do not fork a second component.
2. `dropzone?: boolean` (default `false` — the button path stays the default). When on,
   render a `DropZone` slot (`styled('div', { name: 'EzFileField', slot: 'DropZone' })` with a
   default dashed-border style block a theme can override, `fileFieldClasses.dropZone`,
   `fileFieldClasses.dragActive` toggled while a drag is over it) wrapping the picker button
   and a `dropText` prop (default `'Drag files here, or'` — a prop, i18n-ready). Drop, dragover
   and dragleave handlers feed the same `onChange`/value path as the input. The zone itself is
   **not** focusable and carries no role: the button inside remains the keyboard/screen-reader
   path (parity requirement of the issue), so no new tab stop and no fake `role="button"`.
3. `maxSize?: number` (bytes) and `accept` become validation, not only filtering: a dropped or
   picked file over `maxSize` or outside `accept` (MIME or extension, the same matching the
   native input does) is rejected with `maxSizeMessage`/`acceptMessage` props (defaults
   `'File is larger than {size}'` formatted with a human size, and `'File type not accepted'`)
   surfaced as the field error through the field's rules — composed like `required`, and
   the file is not added to the value. `maxFiles?: number` under `multiple` likewise
   (`maxFilesMessage`, default `'Choose at most {count} files'`). Native `accept` still goes on
   the input.
4. Progress hooks: `renderFile?: (file: File, index: number) => ReactNode` replaces the default
   chip for a file so consumers can show their own upload progress; and `onFilesAdded?:
   (files: File[]) => void` fires once per pick/drop with the accepted files (this is where a
   consumer starts uploads). No upload logic in the library.
5. a11y: the drop zone's text is inside the zone as visible text (not `aria-label`); the
   `dragActive` state is visual only; errors reach `aria-describedby` through the existing
   frame; jest-axe with the zone rendered.
6. Tests: drop adds files (`fireEvent.drop` with a `DataTransfer`-shaped object), dragover
   toggles the class, size/type/count rejections set the field error and leave the value
   unchanged, `renderFile` replaces the chip, `onFilesAdded` payload, button path unchanged,
   theme override reaches the drop zone, axe. Stories: dropzone, with limits, custom
   `renderFile` showing a `LinearProgress` per file (story may style).

### Task 16: Strict mode and a zero-warning build (Steve, Sept 2; supersedes #84)

Files: `tsconfig.json`, `eslint.config.js` (new), `package.json` (scripts, devDeps), `.github/
workflows/*.yml`, `.storybook/main.ts` / `preview.tsx`, `src/test/setup.ts`, `vite.config.ts`,
and whatever the new gates flag across `src/`.

Requirements:
1. **React StrictMode everywhere we render**: Storybook (`reactStrictMode: true` in the
   react-vite framework options) and tests (`configure({ reactStrictMode: true })` from
   Testing Library in `setup.ts`). Fix every double-invoked-effect bug that surfaces (timers,
   subscriptions, focus-on-mount, `useRef`-guarded effects); do not switch StrictMode off for
   individual tests.
2. **Tests fail on console noise**: `setup.ts` patches `console.error` / `console.warn` to
   throw (after the test, so the original assertion failure still shows first) unless the test
   opted in via a small `expectConsole(level, matcher)` helper; migrate the intentional cases
   (`devWarn.test.tsx`, any `act()`-warning tests). Zero silenced tests; fix the source.
3. **TypeScript**: add `noUnusedLocals`, `noUnusedParameters`, `noImplicitReturns`,
   `noFallthroughCasesInSwitch`, `noImplicitOverride`, `forceConsistentCasingInFileNames` to
   the base config (`exactOptionalPropertyTypes` only if MUI's types tolerate it — try it,
   revert with a note if it costs more than ~20 casts). Build config inherits.
4. **ESLint** (flat config): `typescript-eslint` `recommendedTypeChecked` + `stylisticTypeChecked`,
   `eslint-plugin-react-hooks` (rules-of-hooks error, exhaustive-deps error),
   `eslint-plugin-jsx-a11y` recommended, `eslint-plugin-storybook` for stories,
   `eslint-config-prettier` last. `pnpm lint` = `eslint . --max-warnings 0`; run in CI before
   `typecheck`. Fix every finding; disable a rule only with an inline comment saying why, and
   list every such disable in the report.
5. **Builds fail on warnings**: Vite `build.rollupOptions.onwarn` throws (allow-list nothing
   unless documented in the report); Storybook build via `viteFinal` the same; `pnpm
   build-storybook` must produce no `warn` lines (check the CLI output, `--quiet` is not a
   fix). `check-guardrails` unchanged.
6. README Develop section documents the gates; CI workflow runs `lint`, `typecheck`, `test`,
   `test:scripts`, `check:guardrails`, `build`, `build-storybook` in that order.
7. Land as a small number of commits (config, then fixes by area) so the diff reviews.

## Rulings (pre-flight)

- Ruling: one `LiveRegion` component replaces the three ad-hoc live nodes in `src/` — rule 1,
  and #3's issue text asks for the shared utility — cost if wrong: three small migrations to
  revert.
- Ruling: step change focuses the step heading, not the first field — APG multi-step focus
  management; announces context before content — cost if wrong: one extra Tab for keyboard
  users.
- Ruling: `actionsOrder` values are named by button order (`'cancel-confirm'`, `'back-next'`),
  not by platform (`'material'` / `'windows'`) — self-describing, no cultural lookup — cost if
  wrong: rename before publish.
- Ruling: `autoComplete`/`inputMode` derive from `type` on `TextField`, never from `name` —
  a wrong token is worse than none — cost if wrong: consumers set the prop (one attribute).
- Ruling: dev warnings use `control._names.mount` for the wizard check — RHF exposes no
  public "is mounted" API; the set is stable across 7.x — cost if wrong: guard by feature
  detection, warning silently off.
- Ruling: #4 ships a form-level instruction in asterisk mode ON by default via the existing
  `requiredIndicatorText` prop, phrased "Required fields are marked with an asterisk (*)."
  — WCAG 3.3.2 wants the convention stated; `false` turns it off; theme `defaultProps` sets it
  globally — cost if wrong: every existing form gains one line until a consumer passes
  `false`.
- Ruling: no per-field hidden "required" text — `aria-required` already announces — cost if
  wrong: AT that ignores `aria-required` (none current).
- Ruling: #71 keeps the `^18 || ^19` peer range and wraps in `forwardRef` — MUI 9 still
  supports 17–19, so narrowing has no upstream justification — cost if wrong: `forwardRef`
  boilerplate to remove when 18 is dropped.
- Ruling: #10 `ezFormTheme` and #11 reduced-motion, #43 subpath exports, and all
  `needs-design` issues (#14, #20, #21, #23, #47, #65, #79) wait for a brainstorm with Steve
  after this wave — they are taste/packaging decisions — cost if wrong: one wave's delay.
- Ruling: `PhoneField`/`SsnField`/`ZipField` store digits only and format for display —
  zod schemas stay `z.string()` with no regex; the field owns completeness — cost if wrong:
  consumers wanting a formatted value format on submit.
- Ruling: default phone display template is `###-###-####` (Steve's existing message shape),
  configurable via `format` — cost if wrong: one prop.
- Ruling: no masking dependency; a small shared template formatter, caret handling per the
  NumberField technique — rule 1 has nothing upstream to extend here — cost if wrong: an
  imask-class library later.
- Ruling: `AddressField` is a composite of the real part fields under a nested object name
  (`address.street`, …), US-only, Google Places stays #21 — per-part errors and focus come
  free — cost if wrong: a flat-value variant is a later prop.
- Ruling: `StateSelect` ships 50 + DC by default, territories opt-in — cost if wrong: a flag.
- Ruling: Switch is for immediate-effect settings only; in any form with a Submit it is a
  Checkbox — Material selection-controls guidance + `role="switch"` semantics — cost if wrong:
  two example fields flip back.
- Ruling: the drop zone is not a tab stop; the existing button is the keyboard path — the issue
  asks for parity, and a second focusable target would duplicate it — cost if wrong: add
  `tabIndex`/`role` to the zone later.
- Ruling: `maxSize`/`accept`/`maxFiles` reject at pick time and surface through the field's
  rules — mirrors `required` composition — cost if wrong: consumers wanting a soft warning
  use `onFilesAdded`.
- Ruling: strict = React StrictMode + console-noise-fails-tests + stricter tsc + ESLint
  type-checked + builds fail on warnings — Steve: "warnings and errors should fail the build"
  — cost if wrong: a rule or two relaxed with a comment.
