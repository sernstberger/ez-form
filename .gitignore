node_modules
dist
storybook-static
*.log
.DS_Store
.claude/worktrees/
.worktrees/
.playwright-mcp/

# Local secrets (the Storybook-only Google Places key); the example is the documentation.
.env
.env.*
!.env.example

# QA breaker scratch probes (never committed)
src/__qa__/
