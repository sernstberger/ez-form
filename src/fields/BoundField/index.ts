export { BoundField, type BoundFieldProps, type BoundFieldLabelAs, type Bound } from './BoundField'
// Internal: the same frame without `theme.components.EzBoundField.defaultProps`, which
// belong to the public `<BoundField>` alone. The seven non-`TextField` fields render
// through this. Not exported from `src/index.ts`.
export { BoundFieldBase } from './BoundField'
